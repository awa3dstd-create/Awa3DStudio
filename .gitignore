# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/
# OpenNext build artifacts
/.open-next/
/.vercel/

# ============ Secrets & credentials (NEVER COMMIT) ============
# Allow .env.example template, but block real env files
.env
.env.local
.env.production
.env.*.local
!.env.example

# Credentials / recovery files (kept locally only)
docs/04-CREDENTIALS.md
recovery/
scripts/credentials-backup.json
scripts/cf_env_credentials_backup.json
scripts/cf_env_phase2.json
scripts/recover_credentials.py
scripts/build_full_backup.py

# Project lock files for local-only tools
.claude
.z-ai-config

